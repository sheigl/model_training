name: MongoDB MCP
version: 0.0.2
schema: v1
mcpServers:
  - name: MongoDB Server
    command: npx
    args:
      - -y
      - mongodb-mcp-server
      - --connectionString
      - ${{ inputs.MONGO_CONNECTION_STRING }}
      - --readOnly